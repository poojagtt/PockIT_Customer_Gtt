export interface TranslationKeys {
  common: {
    ok: string;
    cancel: string;
    close: string;
    loading: string;
  };
  cart: {
    empty: string;
    addItem: string;
    total: string;
    checkout: string;
  };
}
